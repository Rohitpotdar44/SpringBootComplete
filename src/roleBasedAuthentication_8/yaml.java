package roleBasedAuthentication_8;

public class yaml {
   /*
     YAML
    ain’t markup language   , nothing but it is the markaup language but ulike xml it doesn't has tags it has simple property=value structure

    and class path means that e.g for application prop -> src-> main -> resources and all

    .yml is the extension and it absically reduce the boiler plate of the application.properties

    we can also give commands through cmd by accessing jar

    cmd> appli prop > yml is the priority
    */


}
